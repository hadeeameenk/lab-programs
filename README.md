/*hadee ameen k
ISP*/
#include <stdio.h>
void main()
{
    int l,b,h,vol;
    printf("enter length:\nenter breadth:\nenter height:\n");
    scanf("%d%d%d",&l,&b,&h);
    vol=l*b*h;
    printf("volume of cube is %d",vol);
}
